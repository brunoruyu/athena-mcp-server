# Copy this file to .env and fill in your values for local testing.
# The server reads credentials from HTTP headers, not env vars, so these
# are only used when you wrap the server with a header-injecting proxy or
# craft test requests manually.

AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=us-east-1
# AWS_SESSION_TOKEN=   # optional — only needed for temporary credentials
ATHENA_WORKGROUP=primary
S3_OUTPUT_PATH=s3://my-bucket/athena-results/
